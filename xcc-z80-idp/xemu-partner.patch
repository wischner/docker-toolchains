diff --git a/x/lib/xemu/include/xemu/xemu.h b/x/lib/xemu/include/xemu/xemu.h
index 9ef3a678..5d5b7375 100644
--- a/x/lib/xemu/include/xemu/xemu.h
+++ b/x/lib/xemu/include/xemu/xemu.h
@@ -46,6 +46,7 @@ struct memory_port_rule_config {
     std::string selector;
     uint16_t mask = 0x00FF;
     uint8_t shift = 0;
+    std::optional<uint16_t> fixed_value;
 };
 
 struct memory_map_config {
diff --git a/x/lib/xemu/src/xemu.cpp b/x/lib/xemu/src/xemu.cpp
index 8c3035ac..6b779470 100644
--- a/x/lib/xemu/src/xemu.cpp
+++ b/x/lib/xemu/src/xemu.cpp
@@ -447,6 +447,7 @@ public:
             rule.selector_index = selector_it->second;
             rule.mask = rule_cfg.mask;
             rule.shift = rule_cfg.shift;
+            rule.fixed_value = rule_cfg.fixed_value;
         }
 
         stores_ = std::move(new_stores);
@@ -497,13 +498,24 @@ public:
         compat_selector_index_ = find_selector_index(compat_selector_name);
     }
 
+    void apply_port_read(uint16_t port) noexcept {
+        for (const auto& rule : port_rules_) {
+            if ((port & rule.port_mask) != (rule.port & rule.port_mask)
+                || !rule.fixed_value.has_value()) {
+                continue;
+            }
+            selectors_[rule.selector_index].value = rule.fixed_value.value();
+        }
+    }
+
     void apply_port_write(uint16_t port, uint8_t value) noexcept {
         for (const auto& rule : port_rules_) {
             if ((port & rule.port_mask) != (rule.port & rule.port_mask)) {
                 continue;
             }
-            selectors_[rule.selector_index].value = static_cast<uint16_t>(
-                (static_cast<uint16_t>(value) & rule.mask) >> rule.shift);
+            selectors_[rule.selector_index].value = rule.fixed_value.value_or(
+                static_cast<uint16_t>(
+                    (static_cast<uint16_t>(value) & rule.mask) >> rule.shift));
         }
     }
 
@@ -567,6 +579,7 @@ private:
         std::size_t selector_index = 0;
         uint16_t mask = 0x00FF;
         uint8_t shift = 0;
+        std::optional<uint16_t> fixed_value;
     };
 
     static memory_map_config default_flat_config() {
@@ -705,6 +718,9 @@ struct machine::impl {
         {}
 
         uint8_t in(uint16_t port) noexcept override {
+            if (owner_ != nullptr) {
+                owner_->mem.apply_port_read(port);
+            }
             if (stdin_stream_ != nullptr && stdin_status_port_.has_value() &&
                 port == stdin_status_port_.value()) {
                 const int ch = stdin_stream_->peek();
diff --git a/x/src/xemu/src/main.cpp b/x/src/xemu/src/main.cpp
index 6c58fc77..e535dc0c 100644
--- a/x/src/xemu/src/main.cpp
+++ b/x/src/xemu/src/main.cpp
@@ -50,6 +50,7 @@ struct partial_port_rule_config {
     std::optional<std::string> selector;
     std::optional<uint16_t> mask;
     std::optional<uint8_t> shift;
+    std::optional<uint16_t> fixed_value;
 };
 
 struct memory_map_builder {
@@ -100,7 +101,8 @@ void print_help() {
         << "  --config FILE        load defaults from FILE\n"
         << "                       auto-searches ./xemu.conf then ~/.config/x/xemu.conf\n"
         << "                       advanced banking uses store./window./selector./port_rule. keys\n"
-        << "                       port_rule.*.port_mask can match partially decoded ports\n\n"
+        << "                       port_rule.*.port_mask can match partially decoded ports\n"
+        << "                       port_rule.*.value assigns a fixed selector on IN or OUT\n\n"
         << "options:\n"
         << "  --listen HOST:PORT   listen address (default 127.0.0.1:9000)\n"
         << "  --run                run immediately instead of waiting for a debugger\n"
@@ -353,6 +355,10 @@ bool apply_memory_map_entry(
             rule.shift = static_cast<uint8_t>(parse_u32(value));
             return true;
         }
+        if (parts[2] == "value" || parts[2] == "fixedvalue") {
+            rule.fixed_value = static_cast<uint16_t>(parse_u32(value));
+            return true;
+        }
         throw std::runtime_error("unknown port_rule config key: " + raw_key);
     }
 
@@ -432,6 +438,7 @@ std::optional<xemu::memory_map_config> resolve_memory_map(const memory_map_build
         cfg.selector = rule.selector.value();
         cfg.mask = rule.mask.value_or(0x00FF);
         cfg.shift = rule.shift.value_or(0);
+        cfg.fixed_value = rule.fixed_value;
         config.port_rules.push_back(std::move(cfg));
     }
 
