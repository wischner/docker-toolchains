# Partner Libraries

Place your Iskra Delta Partner runtime library files (`.rel` files) here.

Example files:
- `partner.rel` - Partner system library
- `vdu.rel` - Video display unit library
- `disk.rel` - Disk I/O library
- Any other Partner-specific `.rel` files

These files will be copied to `/opt/partner/lib/` in the Docker image.