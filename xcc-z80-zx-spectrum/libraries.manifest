# Target libraries added to XCC's normal Z80 search path.
# Source refs are pinned to released tags; the exact commit for each is
# recorded under /opt/zx-spectrum/share/metadata in the image.
retro-vault/libgpx v0.4.0 /opt/x/z80/lib/libgpx.a /opt/x/z80/include/libgpx.h
